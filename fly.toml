app = "chat-paper-worker"
primary_region = "nrt"

[build]
  dockerfile = "Dockerfile.worker"

[processes]
  worker = "node dist/server/worker/index.js"

[env]
  NODE_ENV = "production"
  LOG_LEVEL = "info"
  WORKER_CONCURRENCY = "2"
  JOB_TIMEOUT_MS = "600000"

[[vm]]
  memory = "512mb"
